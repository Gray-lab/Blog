set up venv
start new django project
start new django app
add app and import urls
set up model for blog page

most basic:
  -index loads list of blog entries
  -clicking a blog entry goes to a page that displays the content
  -blog entries can be added via admin page